spring:
  application:
    name: order-service
  config:
    import: optional:configserver:http://localhost:8888

server:
  servlet:
    context-path: /ordermgmt/rest

management:
  endpoints:
    web:
      exposure:
        include: "*"