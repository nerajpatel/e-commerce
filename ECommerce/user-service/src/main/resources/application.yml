spring:
  application:
    name: user-service
  config:
    import: optional:configserver:http://localhost:8888

server:
  servlet:
    context-path: /usermgmt/rest

management:
  endpoints:
    web:
      exposure:
        include: "*"